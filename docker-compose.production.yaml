version: "3"
services:
  indexer:
    build:
      context: .
      dockerfile: indexer/Dockerfile
      target: production
    environment:
      ENV: "production"
      # disable sinks
      DATABASE_URL:
      RABBITMQ_SINK_ENABLED: "false"
