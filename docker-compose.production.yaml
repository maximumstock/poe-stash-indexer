version: "3"
services:
  indexer:
    build:
      context: .
      dockerfile: indexer/Dockerfile
      target: production
    environment:
      ENV: "production"
      DATABASE_URL:
      RABBITMQ_SINK_ENABLED: "true"

  trade:
    build:
      context: .
      dockerfile: trade/Dockerfile
      target: production
    environment:
      ENV: "production"
